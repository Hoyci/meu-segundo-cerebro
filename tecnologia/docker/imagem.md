Uma imagem Docker é um arquivo de leitura, chamado de [[Dockerfile]], que contém todas as instruções necessárias para criar um container. 
A imagem é construída usando o comando `docker build` que usa as instruções do arquivo Dockerfile.
Imagens possuem tudo o que a aplicação necessita para rodar, ou seja, kernel, bibliotecas, dependências, código e configurações.
